# SWIMAX sales demo (frontend-only)
#
# No backend or API URL is required. The app uses an in-browser mock API and
# persists demo data under localStorage (see src/demo/constants.js).
#
# This variable is ignored by the demo build but may be kept for tooling compatibility.
VITE_API_BASE_URL=
